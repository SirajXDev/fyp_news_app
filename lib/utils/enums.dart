enum PostApiStatus { initial, loading, success, error }

enum FilterList {
  bbcNews,
  cnn,
  aljazeera,
  aryNews,
}

enum SearchFilter {
  publishedAt,
  relevancy,
  popularity,
}

enum ValidationType {
  username,
  email,
  phoneNo,
}
